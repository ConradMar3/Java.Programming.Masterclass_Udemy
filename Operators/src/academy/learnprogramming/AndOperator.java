package academy.learnprogramming;

public class AndOperator {
    public static void main(String[] args) {
        int topScore = 80;
        int secondScore = 60;
        if (topScore > secondScore && topScore < 100) {
            System.out.println("Top score more than secondScore and more than 100");
        }
    }
}
